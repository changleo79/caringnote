# 🚀 지금 바로 실행하기

Vercel 공식 가이드를 현재 프로젝트에 바로 적용하는 방법입니다.

---

## ⚡ 빠른 실행 (터미널에서 순서대로)

현재 프로젝트 폴더에서 다음 명령어들을 **순서대로** 실행하세요:

### 1️⃣ Vercel CLI 설치 (한 번만)
```bash
npm install -g vercel
```

### 2️⃣ Vercel 로그인 (한 번만)
```bash
vercel login
```
→ 브라우저가 열리면 로그인 버튼 클릭

### 3️⃣ 프로젝트 연결 (한 번만)
```bash
vercel link
```
→ 질문이 나오면:
- **"Set up and develop?"** → `Y` 입력
- **"Link to existing project?"** → `Y` 입력
- **"What's the name?"** → `caringnote` 입력

### 4️⃣ 환경 변수 가져오기
```bash
vercel env pull .env.local
```
→ Vercel에 설정된 환경 변수가 `.env.local` 파일로 생성됩니다

### 5️⃣ Prisma 클라이언트 생성
```bash
npx prisma generate
```

### 6️⃣ 데이터베이스 마이그레이션
```bash
npx prisma migrate dev --name init
```
→ 첫 실행 시 테이블 구조가 데이터베이스에 생성됩니다

### 7️⃣ 시드 데이터 생성
```bash
npm run db:seed
```
또는
```bash
npx prisma db seed
```

### 8️⃣ 배포 (선택사항)
```bash
vercel --prod
```
또는 GitHub에 푸시하면 자동 배포:
```bash
git add .
git commit -m "데이터베이스 마이그레이션 완료"
git push
```

---

## 🎯 한 줄로 복사해서 실행하기

PowerShell이나 터미널에 **한 줄씩** 복사해서 실행하세요:

```bash
npm install -g vercel
```
```bash
vercel login
```
```bash
vercel link
```
```bash
vercel env pull .env.local
```
```bash
npx prisma generate
```
```bash
npx prisma migrate dev --name init
```
```bash
npm run db:seed
```

---

## ✅ 완료 확인

### 1. 환경 변수 확인
```bash
# PowerShell에서
Get-Content .env.local | Select-String "DATABASE_URL"
```

### 2. 데이터베이스 확인
브라우저에서 접속:
```
https://your-app.vercel.app/api/care-centers
```
→ 요양원 목록이 JSON으로 보이면 성공!

### 3. 회원가입 페이지 확인
```
https://your-app.vercel.app/auth/signup
```
→ "요양원 선택" 드롭다운에 요양원 목록이 보이면 완료!

---

## 🆘 문제 해결

### "vercel: command not found"
→ 1단계를 다시 실행: `npm install -g vercel`

### "Error: You are not logged in"
→ 2단계를 다시 실행: `vercel login`

### "Error: This directory is not linked"
→ 3단계를 다시 실행: `vercel link`

### "Migration failed" 또는 "Database connection error"
→ `.env.local` 파일에 `DATABASE_URL`이 있는지 확인:
```bash
Get-Content .env.local
```
없으면 Vercel에서 환경 변수를 먼저 설정해야 합니다.

### "Cannot find module 'tsx'"
→ 다음 실행:
```bash
npm install --save-dev tsx
```

---

## 📝 참고사항

- 각 명령어는 완료될 때까지 기다리세요
- 오류가 나면 메시지를 확인하고 해당 단계를 다시 실행하세요
- `.env.local` 파일은 절대 Git에 커밋하지 마세요!

---

**모든 단계가 완료되면 회원가입이 정상 작동합니다!** 🎉

