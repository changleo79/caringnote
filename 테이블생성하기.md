# 🔧 데이터베이스 테이블 생성하기

## 현재 상황 ✅
- 데이터베이스 연결: **성공** ✅
- DATABASE_URL: **설정됨** ✅
- 문제: **테이블이 없음** ❌

## 해결 방법: 마이그레이션 실행

테이블이 없어서 회원가입이 안 되고 있습니다. 다음 단계로 테이블을 생성하세요.

---

## 🚀 빠른 해결 (3단계)

### 1단계: Vercel CLI로 환경 변수 가져오기

터미널에서 실행:

```bash
# Vercel CLI가 없으면 설치
npm install -g vercel

# 로그인 (한 번만)
vercel login

# 프로젝트 연결 (한 번만)
vercel link

# 환경 변수 가져오기
vercel env pull .env.local
```

### 2단계: 마이그레이션 실행 (테이블 생성)

```bash
npx prisma migrate deploy
```

또는 개발용으로 처음 실행하는 경우:

```bash
npx prisma migrate dev --name init
```

### 3단계: 시드 데이터 생성 (요양원 데이터)

```bash
npm run db:seed
```

---

## 🔍 확인하기

### 테이블 생성 확인
다시 `/api/db-check` 접속:
```
https://caringnote.vercel.app/api/db-check
```

**정상이면:**
- `CareCenter: 0` (테이블 생성됨, 데이터 없음)
- `User: 0` (테이블 생성됨, 데이터 없음)

### 시드 데이터 확인
`/api/care-centers` 접속:
```
https://caringnote.vercel.app/api/care-centers
```

**정상이면:**
- 요양원 목록이 JSON으로 나타남
- 또는 빈 배열 `[]` (시드 데이터 없으면)

---

## 🎯 한 줄씩 복사해서 실행

터미널에서 순서대로:

```bash
vercel env pull .env.local
```
```bash
npx prisma migrate deploy
```
```bash
npm run db:seed
```

---

## ⚠️ 문제 해결

### "vercel: command not found"
```bash
npm install -g vercel
```

### "Error: You are not logged in"
```bash
vercel login
```

### "Error: This directory is not linked"
```bash
vercel link
```

### "Migration failed" 또는 "Table already exists"
→ 이미 테이블이 생성되어 있을 수 있습니다.
→ 시드 데이터만 생성하면 됩니다:
```bash
npm run db:seed
```

---

## ✅ 완료 후

1. `/api/db-check` 확인 → `CareCenter: 0 이상`
2. `/api/care-centers` 확인 → 요양원 목록
3. 회원가입 페이지 → 요양원 선택 가능

**이제 회원가입이 정상 작동합니다!** 🎉

